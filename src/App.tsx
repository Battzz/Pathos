import "./App.css";

function App() {
  return (
    <main className="app-shell">
      <section className="app-panel" aria-label="Application shell">
        <p className="eyebrow">Brush Version 1</p>
        <h1>Conductors</h1>
        <p className="description">
          A stable Tauri + React + Vite + Vitest foundation. The next brush can
          start shaping the real product UI on top of this window.
        </p>
      </section>
    </main>
  );
}

export default App;
